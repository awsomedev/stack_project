import 'package:flutter/material.dart';

TextStyle pulp({double? fontSize, FontWeight? fontWeight, Color? color}) =>
    TextStyle(
      fontSize: fontSize,
      fontWeight: fontWeight,
      color: color,
      fontFamily: 'PulpDisplay',
    );
